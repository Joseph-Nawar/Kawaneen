# Phase 5 chunking manifests

Tracked files in this directory contain only sanitized policy hashes, corpus
scope hashes, counts, integrity rates, and aggregate experiment metrics.

All text-bearing chunks, source spans with private text, challenge queries,
qrels, and per-example results belong under the ignored
`artifacts/private/phase5_chunking/` directory.
