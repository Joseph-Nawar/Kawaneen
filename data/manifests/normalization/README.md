# Phase 4 normalization metadata

This directory contains only sanitized policy hashes, frozen candidate-unit counts,
aggregate diagnostics, retrieval metrics, slice counts, and selection metadata.

Real-data-derived queries, qrels, per-example retrieval results, source-text examples,
collision text, normalized Parquet, and temporary indexes remain ignored under
`artifacts/private/phase4_normalization/`.
