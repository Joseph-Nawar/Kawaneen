# Private parsing benchmark metadata

PDFs, gold text, OCR caches, and review bundles belong under ignored private paths.
Only sanitized benchmark metadata and metrics may be version controlled. No benchmark
fixture or report may contain unmasked legal text or identifiers.
