# Demo shot list

1. Record the public-demo banner and limits before entering any query.
2. Capture the Arabic search query `ما هي مدة الإرجاع؟`, the five-result cap,
   synthetic document title, and exact evidence text.
3. Capture Ask with `ما هي مدة إشعار العقد؟`; expand `E001` and show the quote.
4. Capture Extract with `يلتزم الطرف بالسداد خلال ثلاثين يوماً.`; show the
   deterministic candidate and that upload/Hybrid controls are unavailable.
5. Capture the Evaluation page and retain the Phase 15 negative generator
   result if that page is shown.
6. Capture a terminal containing `docker compose up` and
   `make phase17-space-bundle`, then show the publication status.

No private corpus text, private artifact path, credential, or fabricated
benchmark should appear in the recording.
